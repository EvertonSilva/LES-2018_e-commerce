{{#bulma-hero isPrimary=true isLarge=true class="has-bg-img"}}
  {{#bulma-hero-content}}
    <h1 class="title">Hero Title</h1>
    <h2 class="subtitle">Lorem ipsum dolor sit amet</h2>
  {{/bulma-hero-content}}
{{/bulma-hero}}

{{#bulma-section}}
  {{#bulma-container}}
    <h2 class="title">Featured books</h2>
    <div class="columns is-multiline">
      {{#each model as |bookUnit|}}
        <div class="column is-one-quarter">
          {{book-listing book=bookUnit}}
        </div>
      {{/each}}
    </div>
  {{/bulma-container}}
{{/bulma-section}}
{{outlet}}
